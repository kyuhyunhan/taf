id: C-002
title: Overlay loading label reflects current /analyze phase
source_commit: c0d6872
layer: client
trigger: |
  User has issued ⌘C; overlay is shown in loading state while
  /analyze is in flight.
effect: |
  The label below the spinner updates to a human-readable phase
  string (e.g. "Translating", "Looking up") that matches the
  server's currently-running stage.
boundary: |
  Presentation only. The server contract is unchanged; phase strings
  are derived from streamed telemetry or a fixed sequence — they do
  not block on the response.
